

🧩 Mini Project: Secure Threat Intelligence Client

Create this structure:

    secure_threat_client/
    │
    ├── venv/
    ├── src/
    │   ├── client.py
    │   └── main.py
    ├── .env
    ├── .env.example
    ├── .gitignore
    ├── requirements.txt
    └── README.md

.env
    API_KEY=your_real_key_here
    API_BASE_URL=https://api.example.com

client.py
import os
import requests
from dotenv import load_dotenv

class ThreatIntelClient:
    def __init__(self):
        load_dotenv()

        self.base_url = os.getenv("API_BASE_URL")
        self.api_key = os.getenv("API_KEY")

        if not self.base_url or not self.api_key:
            raise RuntimeError("Required environment variables are missing")

        self.headers = {
            "Authorization": f"Bearer {self.api_key}",
            "Accept": "application/json"
        }

    def lookup(self, indicator):
        response = requests.get(
            f"{self.base_url}/lookup",
            headers=self.headers,
            params={"indicator": indicator},
            timeout=10
        )

        response.raise_for_status()
        return response.json()

Challenge

Add:

    Input validation
    Logging without secrets
    Retry handling for 429
    CSV output
    A clean README
    Unit tests using mocked responses

Use only a legitimate API and test only within its terms and limits.










