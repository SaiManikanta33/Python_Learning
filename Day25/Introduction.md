1. What is a DataBase?

A database is a structured way to store and oorganize data.

Example:

    ID      Username        Status
    1       admin           Active
    2       sai             Active
    3       guest           Disabled

Python includes SQLite, so no extra installation is required.

import sqlite3

